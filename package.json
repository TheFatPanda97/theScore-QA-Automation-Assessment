{
  "name": "thescore-qa-automation-assessment",
  "version": "1.0.0",
  "description": "An k6 testing script for fetch the top 10 teams and players in a given season in a given sports league",
  "main": "src/index.ts",
  "scripts": {
    "test": "npm run build && k6 run build/index.js",
    "lint": "npx eslint .",
    "build": "rm -rf build && tsc --build && node addJSExtension.js"
  },
  "keywords": [
    "TDD",
    "k6.io"
  ],
  "author": "Shawn Hu",
  "license": "MIT",
  "devDependencies": {
    "@types/k6": "^0.45.0",
    "@typescript-eslint/eslint-plugin": "^6.2.0",
    "@typescript-eslint/parser": "^6.2.0",
    "eslint": "^8.45.0",
    "typescript": "^5.1.6"
  }
}
